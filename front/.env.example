# Frontend environment variables (Vite loads only those prefixed with VITE_)
# Copy this file to .env.local (ignored) and fill the actual endpoint.

VITE_AI_ENDPOINT=https://<your-worker-subdomain>.workers.dev
# If your Worker requires a bearer key (optional)
VITE_AI_API_KEY=

# Optional: override default timeout (ms)
VITE_AI_TIMEOUT=10000
