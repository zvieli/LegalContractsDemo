import { describe, it } from 'vitest';

describe('ETH utils', () => {
  it('should parse and format ETH values (skeleton)', () => {
    // TODO:
    // 1. לבדוק המרה מ-ETH ל-wei ולהפך
    // 2. לבדוק טיפול בערכים לא תקינים
    // 3. לבדוק פורמט ערכים לתצוגה ב-UI
  });
});
