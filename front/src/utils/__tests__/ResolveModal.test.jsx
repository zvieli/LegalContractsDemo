import { describe, it } from 'vitest';

describe('EvidencePanel', () => {
  it('should render evidence panel (skeleton)', () => {
    // TODO:
    // 1. לבדוק הצגת evidence digest (hash) תקין ב-UI
    // 2. לבדוק הצגת קישור ל-Helia CID (אם קיים)
    // 3. לבדוק הצגת הודעה כשאין ראיה
    // 4. לבדוק התנהגות עם evidence מוצפן/מקונן
    // 5. לבדוק אינטגרציה עם אירועים מהחוזה (DisputeAppliedCapped, ResolutionApplied)
  });
});
