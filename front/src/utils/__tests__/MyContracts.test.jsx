import { describe, it } from 'vitest';

describe('MyContracts', () => {
  it('should render contracts panel (skeleton)', () => {
    // TODO:
    // 1. לבדוק הצגת חוזים מהזרימה החדשה (digest/Helia)
    // 2. לבדוק הצגת בקשות בוררות עם סטטוס, החלטה, reimbursement
    // 3. לבדוק אינטגרציה עם MetaMask (mock signer)
    // 4. לבדוק התנהגות UI בזמן אמת לפי אירועים
    // 5. לבדוק הצגת הודעה כשאין חוזים
  });
});
