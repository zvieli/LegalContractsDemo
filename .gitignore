# Prevent leaking secrets
start-all.ps1
node_modules
.env

# Hardhat files
/cache
/artifacts

# TypeChain files
/typechain
/typechain-types

# solidity-coverage files
/coverage
/coverage.json

# Hardhat Ignition default folder for deployments against a local node
ignition/deployments/chain-31337

# Frontend generated contract addresses/ABI artifacts (dev-only)
front/src/utils/contracts/

# Admin tooling (do not commit keys)
tools/admin/*.key
tools/admin/*.pem
tools/admin/*.secret
tools/admin/*.env

front/public/utils/contracts/

# Local admin key (do not commit)
admin.key
admin.pub
/evidence_storage
/test-results
/tools/legacy
front/tests/e2e/metamask-extension